[project]
name = "biliup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datarecorder>=3.6.2",
    "downloadkit>=2.0.7",
    "drissionpage>=4.1.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
